# Exemple de configuration pour abis_logs_ingestor
# Copiez ce fichier en `.env` et adaptez les valeurs si nécessaire.

# Racine de stockage (relative au dossier du package ou chemin absolu)
STORAGE_DIR=storage

# Sous-dossiers (relatifs à STORAGE_DIR)
# - INPUTS_SUBDIR     : logs bruts à ingérer (contient aussi `processing_data/` pour les .log décompressés)
# - ARCHIVES_SUBDIR   : racine des archives (contient `inputs/<YYYYMMDD>/` et `json_data/<YYYYMMDD>/`)
# - ERRORS_SUBDIR     : logs problématiques non parsés
# - LOGS_SUBDIR       : fichiers de statistiques d'exécution (regroupés par date : `logs/<YYYYMMDD>/`)
# - JSON_DATA_SUBDIR  : JSON générés avant persistance en base (puis archivés dans `archives/json_data/<YYYYMMDD>/`)
INPUTS_SUBDIR=inputs
ARCHIVES_SUBDIR=archives
ERRORS_SUBDIR=errors
LOGS_SUBDIR=logs
JSON_DATA_SUBDIR=json_data

# Configuration base de données (PostgreSQL / SQLAlchemy)
# Soit une URL/DSN complète (recommandé) :
# DB_DSN=postgresql+psycopg2://user:password@localhost:5432/abis
#   ou, pour compatibilité avec psql :
# DB_DSN=postgresql://user:password@localhost:5432/abis
#
# Soit les champs séparés (utilisés si DB_DSN est vide) :
DB_HOST=localhost
DB_PORT=5432
DB_NAME=abis
DB_USER=postgres
DB_PASSWORD=


